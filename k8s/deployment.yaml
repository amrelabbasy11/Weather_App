apiVersion: apps/v1
kind: Deployment
metadata:
  name: weather-app
spec:
  replicas: 2  # Run 2 instances
  selector:
    matchLabels:
      app: weather-app
  template:
    metadata:
      labels:
        app: weather-app
    spec:
      containers:
        - name: weather-container
          image: amrelabbasy11/weather-app:latest  # Your app's container
          ports:
            - containerPort: 80  # The port inside the container
